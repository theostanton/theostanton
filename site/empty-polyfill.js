// Intentionally empty. The Next.js polyfill-module is replaced at build
// time because the browserslist targets only support modern browsers that
// already implement the polyfilled features natively. See next.config.js.
