{
  "name": "theo.dev",
  "private": true,
  "version": "1.0.2",
  "repository": "https://github.com/theostanton/theostanton.git",
  "author": "Theo <theo@theo.dev>",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "watch": "yarn transpile && yarn workspace @stats/model watch & yarn workspace @stats/database watch & yarn workspace @stats/notion watch & yarn workspace @stats/client watch & yarn workspace @stats/controllers watch",
    "transpile": "yarn workspace @stats/model transpile && yarn workspace @stats/database transpile && yarn workspace @stats/notion transpile && yarn workspace @stats/client transpile && yarn workspace @stats/controllers transpile"
  },
  "devDependencies": {
    "@types/node": "^25.5.1",
    "@types/react": "^19.2.14",
    "typescript": "^5.8.3"
  },
  "workspaces": [
    "stats/*",
    "site"
  ]
}
